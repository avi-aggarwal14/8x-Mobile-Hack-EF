# Copy this file to `.env` and fill in real values. NEVER put real secrets in this file.
# All keys below are SERVER-SIDE only — do not expose to the browser / no NEXT_PUBLIC_ prefix.

# Anthropic Claude API — powers the Goose engine
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# Telegram bot (from @BotFather) — Goose's "it texts you" delivery channel
TELEGRAM_BOT_TOKEN=
