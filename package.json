{
  "private": true,
  "name": "goose-8x",
  "description": "Goose — serverless functions for the static PWA (Vercel /api). No build step; deps are only for the functions.",
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "web-push": "^3.6.0"
  }
}
